<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD</title>
</head>
<body>
    
<?php
$servername = "localhost";
$username = "root";
$password ="";
$dbname = "myDB";


$conn = new mysqli($servername, $username, $password, $dbname);

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$sql="SELECT * FROM member";
$result = $conn->query($sql);
if ($result->num_rows > 0) {
    
    while($row = $result->fetch_assoc()) { 
        echo "<br> id: ". $row["id"]. " - Name: ". $row["name"]. "    -Code:  " . $row["code"] . " -Mobile Number:  " . $row["mobilenum"] . "<br>";
    }
} else {
    echo "0 results";
}
$conn->close();
?>  
</body>
</html>



<!-- <?php
include 'db.php';

if ($_SERVER['REQUEST_METHOD'] == 'POST') {
    $action = $_POST['action'];
    $id = isset($_POST['id']) ? intval($_POST['id']) : 0;
    $name = isset($_POST['name']) ? $conn->real_escape_string($_POST['name']) : '';
    $code = isset($_POST['code']) ? $conn->real_escape_string($_POST['code']) : '';
    $mobilnum = isset($_POST['mobilnum']) ? $conn->real_escape_string($_POST['mobilnum']) : '';

    if ($action == 'Insert') {
        $sql = "INSERT INTO contacts (name, code, mobilnum) VALUES ('$name', '$code', '$mobilnum')";
        if ($conn->query($sql) === TRUE) {
            echo "New record created successfully";
        } else {
            echo "Error: " . $sql . "<br>" . $conn->error;
        }
    } elseif ($action == 'Update' && $id > 0) {
        $sql = "UPDATE contacts SET name='$name', code='$code', mobilenum='$mobilenum' WHERE id=$id";
        if ($conn->query($sql) === TRUE) {
            echo "Record updated successfully";
        } else {
            echo "Error: " . $sql . "<br>" . $conn->error;
        }
    } elseif ($action == 'Delete' && $id > 0) {
        $sql = "DELETE FROM contacts WHERE id=$id";
        if ($conn->query($sql) === TRUE) {
            echo "Record deleted successfully";
        } else {
            echo "Error: " . $sql . "<br>" . $conn->error;
        }
    } elseif ($action == 'Edit' && $id > 0) {
        $result = $conn->query("SELECT * FROM contacts WHERE id=$id");
        if ($row = $result->fetch_assoc()) {
            echo "<form method='POST' action='process.php'>";
            echo "<input type='hidden' name='id' value='{$row['id']}'>";
            echo "<label for='name'>Name:</label>";
            echo "<input type='text' id='name' name='name' value='{$row['name']}' required>";
            echo "<br>";
            echo "<label for='code'>Code:</label>";
            echo "<input type='text' id='code' name='code' value='{$row['code']}' required>";
            echo "<br>";
            echo "<label for='mobilnum'>Mobile Number:</label>";
            echo "<input type='text' id='mobilenum' name='mobilenum' value='{$row['mobilnum']}' required>";
            echo "<br>";
            echo "<input type='submit' name='action' value='Update'>";
            echo "</form>";
        }
    }
}

$conn->close();
?> -->